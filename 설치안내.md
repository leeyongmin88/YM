# 설치 안내 — YM 데일리 리포트 자동화

이 폴더만 있으면 리포트 생성이 그대로 돌아갑니다. 아래 3단계만 하면 됩니다.

---

## 1. 폴더 배치

압축을 풀어서 **`C:\Users\<본인계정>\YM`** 에 둡니다.
(다른 위치에 둬도 동작합니다. 경로가 코드에 박혀 있지 않습니다. 단, 아래 안내는 이 경로 기준입니다.)

## 2. Python 3.12 설치

> 처음 한 번만 하면 됩니다. 10분이면 끝납니다.
> **버전이 중요합니다. 반드시 3.12를 설치하세요.** 최신 버전(3.13 등)을 받으면 안 됩니다.

### 2-1. 이미 깔려 있는지 먼저 확인

키보드 **윈도우키 + R** → `cmd` 입력 → 엔터. 검은 창이 뜨면 아래를 입력합니다.

```
py -3.12 --version
```

`Python 3.12.10` 처럼 나오면 **이미 설치된 것입니다. 2-4번으로 바로 넘어가세요.**
`찾을 수 없습니다` 같은 메시지가 나오면 아래 2-2로 진행합니다.

### 2-2. 내려받기

1. https://www.python.org/downloads/release/python-31210/ 접속
2. 페이지를 **맨 아래까지** 스크롤하면 표가 나옵니다
3. 표에서 **`Windows installer (64-bit)`** 를 클릭 (약 25MB)
4. 다운로드된 `python-3.12.10-amd64.exe` 를 더블클릭

### 2-3. 설치 화면에서 체크박스 두 개

첫 화면 아래쪽에 체크박스가 두 개 있습니다. **여기가 제일 중요합니다.**

| 체크박스 | 어떻게 |
|---|---|
| `Use admin privileges when installing py.exe` | **체크 안 함** (비워둡니다) |
| `Add python.exe to PATH` | **체크함** ✅ |

그다음 **`Install Now`** 를 클릭합니다.
(`Customize installation` 은 누르지 마세요. 설치 위치가 바뀝니다.)

설치가 끝나고 `Setup was successful` 이 나오면 **Close** 를 누릅니다.

> **왜 체크박스가 중요한가**
> 실행 배치파일은 파이썬을 먼저 기본 위치에서 찾고,
> 거기 없으면 `py` 런처로 다른 위치까지 자동으로 찾아냅니다.
> 그래서 `Add python.exe to PATH` 만 체크돼 있으면 어디에 설치하든 동작합니다.
> 이 체크를 빠뜨리면 자동 탐색이 안 되므로, 설치할 때 꼭 체크하세요.

### 2-4. 라이브러리 설치

파이썬만 깔면 아직 안 됩니다. 리포트를 만드는 데 쓰는 도구 두 개(`pandas`, `openpyxl`)를 더 받아야 합니다.

**① `YM` 폴더를 탐색기로 엽니다.**

폴더 안에 `1_리포트_생성.bat` 같은 파일들이 보이는 화면이어야 합니다.

**② 위쪽 주소창을 클릭합니다.**

`> 내 PC > 로컬 디스크(C:) > 사용자 > ...` 이렇게 적힌 긴 막대입니다.
클릭하면 파란색으로 선택되면서 `C:\Users\내계정\YM` 같은 글자로 바뀝니다.

**③ 그 상태에서 `cmd` 라고 치고 엔터를 누릅니다.**

원래 있던 글자는 지우고 `cmd` 세 글자만 남기면 됩니다.
검은 창이 뜨는데, 맨 아랫줄이 이렇게 되어 있으면 성공입니다.

```
C:\Users\내계정\YM>
```

> 이 방법을 쓰는 이유는, 검은 창이 **처음부터 `YM` 폴더 안에서** 열리기 때문입니다.
> 폴더를 찾아 들어가는 명령을 따로 칠 필요가 없습니다.

**④ 아래 한 줄을 복사해서 붙여넣고 엔터를 누릅니다.**

```
py -3.12 -m pip install -r requirements.txt
```

> 검은 창에 붙여넣기는 **마우스 우클릭** 입니다. `Ctrl+V` 가 안 먹을 수 있습니다.

**⑤ 글자가 쭉 올라가다가 멈추면 끝입니다.**

마지막에 아래 중 하나가 나오면 성공입니다.

- `Successfully installed pandas-3.0.3 openpyxl-3.1.5 ...` → 새로 설치됨
- `Requirement already satisfied: ...` → 이미 깔려 있음 (이것도 정상입니다)

`[notice] A new release of pip is available` 같은 문구는 그냥 알림이니 무시하세요.

### 2-5. 잘 됐는지 확인

**같은 검은 창에** 아래를 붙여넣고 엔터를 누릅니다.

```
py -3.12 -m pip list
```

쭉 나오는 목록 안에 **`openpyxl`** 과 **`pandas`** 두 개가 보이면 성공입니다.
(가나다 순으로 정렬돼 있어서 `o`, `p` 근처를 보시면 됩니다.)

확인이 끝나면 검은 창은 `exit` 를 치거나 X 버튼으로 닫으면 됩니다.

### 파이썬 설치가 안 될 때

| 증상 | 해결 |
|---|---|
| `py`를 찾을 수 없다고 나옴 | 설치할 때 `Add python.exe to PATH` 를 체크 안 한 것입니다. 설치 파일을 다시 실행해서 `Modify` 로 다시 설치하세요 |
| `pip` 에서 네트워크 오류 | 회사 보안 프로그램이 막는 경우가 많습니다. 사내망 대신 개인 핫스팟으로 시도해 보세요 |
| `requirements.txt를 찾을 수 없음` | 검은 창이 `YM` 폴더에서 열리지 않았습니다. 창 맨 아랫줄이 `...\YM>` 로 끝나는지 보고, 아니면 2-4의 ①~③을 다시 하세요 |
| `[오류] Python 3.12 를 찾지 못했습니다` | 파이썬이 아예 없거나 3.12가 아닙니다. 2-1부터 다시 하세요 |
| 배치파일 창이 뜨자마자 닫힘 | 파이썬 문제가 아닙니다. 파일을 우클릭 → **편집**으로 열리는지 먼저 확인하세요 |

## 3. 실행

| 배치파일 | 하는 일 |
|---|---|
| `1_리포트_생성.bat` | **메인.** 월 선택 → `output/통합_리포트_....xlsx` (32시트) 생성 |
| `2_통합리포트_생성.bat` | 여러 달 통합 리포트 |
| `3_애드코드연결.bat` | 애드코드 사전 기준 속성 연결 |

더블클릭 → 월 번호 선택 → Enter. 끝나면 `output/` 폴더에 파일이 생깁니다.

> ⚠️ 생성하려는 파일이 **엑셀에서 열려 있으면 실패**합니다(파일 잠김). 닫고 다시 실행하세요.

---

## 매월 하는 일

1. `Raw_N_YYYY_MM` 형식으로 **새 월 폴더**를 만들고, 지난달 폴더의 하위 구조를 그대로 복사
   (`Criteo · GA · Google · KKO · Meta · Meta_ENG · NAV · NSA · RTB · Dable · TikTok · Toss`)
2. 매체별 RAW 파일을 각 폴더에 넣기 (파일명은 지난달과 동일하게)
3. **`예산.xlsx`** 의 `MI / EBM / IT` 금액을 이번 달 값으로 수정
   - 이 파일 하나만 고치면 정액 집행액 · Total 집행율 · 브랜드종합 집행율에 전부 반영됩니다
   - 매체를 추가하려면 한 줄 추가: `구분 | 매체 | 통합매체 | 패턴 | MI | EBM | IT`
4. `1_리포트_생성.bat` 실행 → 새 월 폴더 번호 선택

대상 연·월은 데이터에서 **자동 감지**되므로 코드를 고칠 일이 없습니다.

---

## Claude Code 연동 (권장)

리포트 수정·디버깅·새 요구사항 반영을 클로드에게 시킬 수 있습니다.
이 폴더에는 **프로젝트 지침(`CLAUDE.md`)과 리포트 명세 스킬(`.claude/skills/`)이 이미 들어있어서**,
`YM` 폴더에서 클로드를 실행하면 프로젝트 맥락을 자동으로 읽습니다.

### 준비

1. **Claude 계정** — Claude Pro 또는 Max 구독 (본인 계정 필요)
2. **설치** — 아래 중 편한 것 하나

   - **VS Code / Cursor 확장**: 확장 마켓플레이스에서 `Claude Code` 검색 → 설치
   - **데스크톱 앱**: https://claude.ai/download
   - **터미널(CLI)**: PowerShell에서
     ```powershell
     npm install -g @anthropic-ai/claude-code
     ```
     (Node.js 18+ 필요)

3. **실행** — 반드시 `YM` 폴더를 열거나, 그 폴더로 이동해서 실행

   ```powershell
   cd $env:USERPROFILE\YM
   claude
   ```
   VS Code라면 `File > Open Folder`로 `YM` 폴더를 열고 클로드 패널을 띄우면 됩니다.

4. 첫 실행 시 브라우저가 열리면서 로그인 → 승인하면 연동 완료

### 이렇게 물어보면 됩니다

- `8월 리포트 생성해줘`
- `Total 시트 요일별 평균에 세션수 컬럼 추가해줘`
- `크리테오 광고비가 참고파일이랑 안 맞아. 원인 찾아줘`
- `예산.xlsx에 매체 한 줄 추가했는데 리포트에 안 나와`

### 알아둘 것

- 폴더 안의 `CLAUDE.md` 에 **프로젝트 규칙과 과거에 겪은 함정**이 정리돼 있습니다.
  (색상 알파 문제, GA CSV 헤더 밀림, Criteo read_only 등 — 다시 겪지 않도록)
- 클로드가 코드를 고치면 **리포트를 재생성해야** 반영됩니다.
  엑셀에서 직접 고친 내용은 재생성 시 **덮어써집니다.** 확정된 디자인은 "코드에 반영해줘"라고 요청하세요.
- 클로드는 `pipeline/` 의 파이썬 코드를 고치는 방식으로 일합니다. 엑셀을 직접 조작하지 않습니다.

---

## 문제가 생기면

| 증상 | 원인 / 해결 |
|---|---|
| `PermissionError` | 대상 xlsx가 엑셀에 열려 있음 → 닫고 재실행 |
| `[오류] Python 3.12 를 찾지 못했습니다` | Python 3.12 미설치. 「2. Python 3.12 설치」 참고 |
| 한글이 깨져 보임 | 배치파일로 실행하면 자동 처리됨. 수동 실행 시 `$env:PYTHONIOENCODING="utf-8"` |
| 특정 매체 수치가 0 | 해당 월 폴더에 RAW 파일이 없거나 파일명이 다름 |
| 집행율이 이상함 | `예산.xlsx` 의 이번 달 금액 확인 |
